import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'MyTrainingApp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
